# Reply (elder)

The scion's probe questions arrived as your wake message. Answer each briefly from what you already know.

Then hand off: send your reply to the scion as the message body (summary `quickening → run end`), re-arm the watcher, and report the handoff result to the user. Your phases are over, but stay available: answer any late questions that arrive — reply to the sender's `from` (if `send` reports no mailbox for a same-folder peer, `flbus mailbox add <from>` first) — until the user closes this session.
